const starter = require('starter');
const observers = require('observers');

console.log('===========aaya ================in main',process.env.NODE_PATH)
const starterMain = starter.starter;
const observerMain = observers.starter;

